The project is written in haskell, using some of (my own) code from the interpreter assignment. 

I used cabal, so all of the project specifications are listed in instant231015.cabal

required haskell libs: 

    build-depends:    array
        , base >=4.7 && <5
        , containers
        , mtl >= 2.2
        , lens >= 4.19
        , text
        , filepath
        , process
