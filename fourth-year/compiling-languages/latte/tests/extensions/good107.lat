int main() {
    string[] arr = new string[20]; 
    int i = 1;
    string s = "hej";
    while (i < 20) {
        arr[i] = s + s;
        printString(arr[i]);
        i = i + 2;
    }
    printInt(arr.length);
    return 0;
}

