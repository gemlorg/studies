int main() {
    string[] arr = new string[20];
    int i = 1;
    arr[0] = "a";
    while (i < 20) {
        arr[i] = arr[i-1] + "a";
        printString(arr[i]);
        printInt(i);
        i = i + 2;
    }
    return 0;
}

