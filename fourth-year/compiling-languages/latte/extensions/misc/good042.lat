
int main() {
    int size = 6;
    int[] arr = new int[size];
    int i = 2;
    arr[0] = 0;
    arr[1] = 1;

    while (i < size) {
        arr[i] = arr[i-1] + arr[i-2];
        i++;
    }
    for (int a: arr) {
        printInt(a);
        return 0;
    }

    return 0;
}
