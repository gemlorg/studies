
int main() {
    int size = 6;
    string[] arr = new string[size];

    printString("Fibo strings:");
    int i = 2;
    arr[0] = "";
    arr[1] = "1";
    while (i < size) {
        arr[i] = arr[i-1] + arr[i-2];
        i++;
    }

    for (string s: arr) {
        printString(s);
    }

    return 0;
}
